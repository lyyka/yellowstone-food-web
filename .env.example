XAI_API_KEY=
XAI_MODEL=grok-4
XAI_IMAGE_MODEL=grok-imagine-image-2.0
XAI_IMAGE_QUALITY=low
FAL_KEY=
FAL_IMAGE_MODEL=fal-ai/flux/schnell
# Absolute site origin for Open Graph / Twitter previews (no trailing slash).
VITE_PUBLIC_ORIGIN=
# Production API origin for the static UI (no trailing slash). Local Vite still proxies /api.
VITE_API_URL=
